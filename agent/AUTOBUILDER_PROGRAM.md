# AUTOBUILDER PROGRAM — coda-sweep

Generated by autobuilder Stage 2. Read-only after scaffold.

## Intent

Build the `coda` workspace: shared types + `LogStore` trait + pure `sweep()` +
`CodaConfig` + `coda plan` CLI. All 8 ACs must be green. No `--apply` flag in
this PRD.

## Ownership

- `src/` — edit-agent edits only here.
- `tests/acceptance_*.rs` — one test per AC; body editable, `//!` header read-only.
- Everything else — read-only (see `agent/owner-map.json`).

## Advance criteria

All of the following must be green to advance an iteration:
1. `cargo check --workspace`
2. `cargo clippy --workspace -- -D warnings`
3. `cargo test --workspace`
4. `cargo deny check bans licenses sources`

## Non-goals (locked)

- No `--apply` flag or actual rendering.
- No `FsStore` (real filesystem); that is coda-audit.
- No daemon, scheduler, or watcher.
- No agorabus integration.
